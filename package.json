{
  "name": "egm-downloader",
  "version": "1.3.12",
  "private": true,
  "description": "EGM Downloader web UI — Flask app you can preview in a browser or self-host",
  "repository": {
    "type": "git",
    "url": "https://github.com/zamasshange/downloader.git"
  },
  "scripts": {
    "setup": "uv venv .venv --python 3.12 && uv pip install --python .venv/bin/python -r requirements.txt -r requirements-web.txt",
    "dev": "EGM_DEV_MODE=1 PORT=8080 FLASK_HOST=127.0.0.1 .venv/bin/python app.py",
    "start": "EGM_DEV_MODE=1 PORT=8080 .venv/bin/gunicorn --bind 127.0.0.1:8080 --threads 8 --timeout 120 wsgi:app"
  }
}
